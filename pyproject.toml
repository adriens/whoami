[project]
name = "whoami"
version = "0.1.0"
description = "CV Adrien Sales — validation et outillage"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jsonschema>=4.26.0",
    "requests>=2.34.2",
]
